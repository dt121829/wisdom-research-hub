# Copy this file to secrets.toml (same folder) and fill in the values.
# secrets.toml is git-ignored — never commit it.
#
# On Streamlit Community Cloud, don't upload this file: paste the same contents
# into the app's Settings -> Secrets box instead.

# --- Copilot (Azure OpenAI) --------------------------------------------------
# Powers the market outlook, news digest, buyside extraction, report generator
# and research assistant. Find these in the Azure portal under your Azure OpenAI
# resource: Keys and Endpoint, plus the name you gave your model deployment.
AZURE_OPENAI_ENDPOINT = "https://diegotsai-3087-resource.services.ai.azure.com/"
AZURE_OPENAI_API_KEY = "Fb4Tdi90lbCVTa9aQBRKjjvV9EoDcTpKgIMVwfDCNessHXyEp9ziJQQJ99CGACMsfrFXJ3w3AAAAACOGkqWQ"
AZURE_OPENAI_DEPLOYMENT = "gpt-5-mini-1"
# AZURE_OPENAI_API_VERSION = "2024-10-21"   # optional, only if you need to pin it

# --- Claude (optional fallback provider) -------------------------------------
# If Azure is not configured but this is, the app uses Claude instead.
# ANTHROPIC_API_KEY = "sk-ant-..."

# --- Access control ----------------------------------------------------------
# Optional. If set, visitors must enter this password before seeing the app.
# Leave it out entirely for an open instance (e.g. local development).
# APP_PASSWORD = "choose-a-strong-shared-password"
